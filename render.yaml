services:
  - type: web
    name: agent-base
    env: python
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd backend && HOST=0.0.0.0 python server.py
    envVars:
      - key: LLM_PROVIDER
        value: mock

